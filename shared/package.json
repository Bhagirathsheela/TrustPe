{
  "name": "trustpe-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared zod schemas, TypeScript types, and constants for backend, mobile, and admin.",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
