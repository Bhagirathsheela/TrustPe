{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && npm install -g bun && bun install --frozen-lockfile",
  "buildCommand": "cd .. && bun run --cwd admin build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["bom1"]
}
