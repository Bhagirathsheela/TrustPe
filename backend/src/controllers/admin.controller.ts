/**
 * Deprecated placeholder — admin endpoints are split per resource. The current
 * canonical controllers are:
 *   - admin-kyc.controller.ts  (KYC review)
 * Additional admin controllers (users, disputes, etc.) will land in later sprints.
 */
export {};
