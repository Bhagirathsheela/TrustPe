/**
 * Deprecated placeholder — admin services are split per resource. The current
 * canonical services are:
 *   - admin-kyc.service.ts  (KYC review queue + decisions)
 * Additional admin services (user management, disputes, etc.) will land in
 * later sprints.
 */
export {};
